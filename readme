- Install python.
- pip install pyautogui  --> script uses this library
- pip install pyinstaller  --> to compile
- pip install PyQt5  --> compilation needs this library
- pyinstaller -w unlock.py  --> this will compile
